import React from "react";

const RegisterUser = () => {
  const [register, setRegister] = useState({
      username: "",
      password: "",
      phoneNumber: ""
  });
};

export default RegisterUser;
